---
title: 'Cosmos Hub Testnet snapshot'
sidebarTitle: 'Cosmos Testnet'
---

<Note>
  Coming soon. We run a Cosmos Hub testnet (`provider`) node as part of the Hypha Testnet
  Incentives Program, and a public snapshot for it will be published here on the same 4-hour
  rotation as the mainnet snapshot.
</Note>

Once live, this page will document downloading and restoring a Cosmos Hub **testnet** (`provider`)
snapshot, mirroring the [Cosmos Mainnet](/snapshots/cosmos-mainnet) flow (`gaiad`, `$HOME/.gaia`,
`data/`-only archive, `priv_validator_state.json` excluded).

Meanwhile, the mainnet snapshot is available now: [Cosmos Mainnet](/snapshots/cosmos-mainnet).
